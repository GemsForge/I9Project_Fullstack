export default interface User {
    id: any | null,
    firstName: string,
    lastName: string,
    username: string | null,
    email: string,
    password: string,
    // roles?: Array<string>
   
  }