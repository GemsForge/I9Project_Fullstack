import { Header } from "semantic-ui-react";

export default function TestBanner(){
return(
    <Header as='h1' content='This is a Test' subheader='This UI is used to test the UI client request to the server for all AppUser related requests.'/>
)
}